@host = http://localhost:3000
@apiPrefix = api
@baseUrl = {{host}}/{{apiPrefix}}
@accessToken = replace_with_access_token
@workerCallbackToken = replace_with_worker_callback_token

### Enqueue scrape (auth)
POST {{baseUrl}}/job-sources/scrape
Authorization: Bearer {{accessToken}}
Content-Type: application/json

{
  "source": "pracuj-pl",
  "limit": 20,
  "filters": {
    "specializations": ["frontend"],
    "workModes": ["home-office", "hybrid", "full-office"],
    "keywords": "angular"
  }
}

### Worker completion callback (internal)
POST {{baseUrl}}/job-sources/complete
Authorization: Bearer {{workerCallbackToken}}
Content-Type: application/json

{
  "sourceRunId": "replace_with_run_id",
  "runId": "replace_with_worker_run_id",
  "jobCount": 5
}