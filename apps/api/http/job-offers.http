@host = http://localhost:3000
@apiPrefix = api
@baseUrl = {{host}}/{{apiPrefix}}
@accessToken = replace_with_access_token

### List job offers (auth)
GET {{baseUrl}}/job-offers?status=NEW&minScore=0&limit=20&offset=0
Authorization: Bearer {{accessToken}}

### Update job offer status (auth)
PATCH {{baseUrl}}/job-offers/replace_with_user_job_offer_id/status
Authorization: Bearer {{accessToken}}
Content-Type: application/json

{
  "status": "SAVED"
}

### Score job offer with LLM (auth)
POST {{baseUrl}}/job-offers/replace_with_user_job_offer_id/score
Authorization: Bearer {{accessToken}}
Content-Type: application/json

{
  "minScore": 60
}