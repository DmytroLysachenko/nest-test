{
  "extends": "./tsconfig.base.json",
  "display": "Base Library",

  /*
   * =====================================================================
   * TypeScript library config
   * Extends tsconfig.base.json for library development
   * =====================================================================
   *
   * React/JSX support:
   *   - jsx                - use the React 17+ JSX transform
   *   - lib                - include ES2022, DOM, and DOM.Iterable typings
   *
   * Library build options:
   *   - declaration        - generate .d.ts type declarations
   *   - moduleResolution   - use bundler module resolution
   *   - declarationMap     - generate declaration source maps
   *
   * Monorepo support:
   *   - composite          - enable incremental builds (optional)
   * =====================================================================
   */

  "compilerOptions": {
    // React/JSX support
    "jsx": "react-jsx",
    "lib": ["es2022", "DOM", "DOM.Iterable"],

    // Library build options
    "declaration": true,
    "moduleResolution": "bundler",
    "declarationMap": true

    // Monorepo support (optional)
    // "composite": true
  }
}
