{
  "$schema": "https://json.schemastore.org/tsconfig.json",
  "display": "Base",

  /*
   * =====================================================================
   * TypeScript base config
   * Strict settings for modern TypeScript projects
   * =====================================================================
   *
   * Base options:
   *   - esModuleInterop      - allow default imports from non-ES modules
   *   - skipLibCheck         - skip type checks for library files
   *   - target               - output JavaScript version (ES2022)
   *   - resolveJsonModule    - allow importing JSON modules
   *   - moduleDetection      - force module detection
   *   - isolatedModules      - compile each file as a separate module
   *   - verbatimModuleSyntax - preserve import/export syntax
   *   - useDefineForClassFields - recommended to set true
   *
   * Strict mode:
   *   - strict                     - enable all strict type checks
   *   - noUncheckedIndexedAccess   - index signatures include undefined
   *   - noImplicitOverride         - require override keyword for overrides
   *   - noUncheckedSideEffectImports - prevent unchecked side-effect imports
   *   - erasableSyntaxOnly         - 5.8.3+ for runtime-compatible TS
   *
   * Performance:
   *   - incremental - enable incremental compilation
   *
   * No-emit mode:
   *   - module  - preserve module type
   *   - noEmit  - do not output compiled files
   * =====================================================================
   */

  "compilerOptions": {
    // Base options
    "esModuleInterop": true,
    "skipLibCheck": true,
    "target": "esnext",
    "resolveJsonModule": true,
    "moduleDetection": "force",
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "useDefineForClassFields": true,

    // Strict mode
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noUncheckedSideEffectImports": true,

    // Performance
    "incremental": true,

    // No-emit mode
    "module": "preserve",
    "noEmit": true
  }
}
